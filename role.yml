apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-reader
  namespace: apache

rules:
- apiGroups: ["", "apps"]
  resources: ["pods", "deployments"]
  verbs:
  - get
  - list
  - watch
